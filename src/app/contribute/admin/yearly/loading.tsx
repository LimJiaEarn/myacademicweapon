import FormPageSkeleton from "@/components/shared/FormPageSkeleton";

// /contribute/admin/yearly — single-line title + 13-field form
export default function Loading() {
  return <FormPageSkeleton fields={13} />;
}
