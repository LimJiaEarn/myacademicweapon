import FormPageSkeleton from "@/components/shared/FormPageSkeleton";

// /contribute/admin/topical — single-line title + 11-field form
export default function Loading() {
  return <FormPageSkeleton fields={11} />;
}
