import FormPageSkeleton from "@/components/shared/FormPageSkeleton";

// /contribute/admin/notes — single-line title + 8-field form
export default function Loading() {
  return <FormPageSkeleton fields={8} />;
}
